---
version: 5
defaults:
  datadir: 'hieradata'
  data_hash: yaml_data
hierarchy:
  - name: "APPTIER"
    path: "%{project}/%{appname}/%{apptier}/%{apptier}.yaml"
  - name: "APPNAME"
    path: "%{project}/%{appname}/%{appname}.yaml"
  - name: "PROJECT"
    path: "%{project}/%{project}.yaml"
  - name: "Common Stuff"
    path: "common.yaml"
